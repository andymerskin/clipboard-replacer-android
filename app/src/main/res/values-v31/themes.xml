<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash uses these; keep background black to match the cold start window. -->
    <style name="Theme.ClipboardReplacer" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
    </style>
</resources>
